txtbox = document.getElementById('txtbox');

function myfunc() {
txtbox = '3200';
console.log(parseInt(txtbox));
    if(txtbox.value = 3200) {
        alert("Specter, full shields, one of each ability");
    }
}